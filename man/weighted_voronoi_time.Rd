% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weighted_voronoi_time.R
\name{weighted_voronoi_time}
\alias{weighted_voronoi_time}
\title{Temporal weighted tessellation}
\usage{
weighted_voronoi_time(
  points_list,
  weight_col,
  boundary_sf,
  time_index = NULL,
  distance = c("euclidean", "geodesic"),
  geodesic_engine = c("multisource", "classic"),
  resistance_list = NULL,
  dem_list = NULL,
  keep_polygons = FALSE,
  keep_summaries = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{points_list}{A non-empty list of \code{sf} POINT objects, one per time step.}

\item{weight_col}{Character. Name of the weight column present in each element
of \code{points_list}.}

\item{boundary_sf}{An \code{sf} POLYGON/MULTIPOLYGON defining the domain.}

\item{time_index}{Optional character vector of time labels. Defaults to
\code{names(points_list)} if present, otherwise \code{"t1"}, \code{"t2"}, etc.}

\item{distance}{Character. One of \code{"euclidean"} or \code{"geodesic"}.}

\item{geodesic_engine}{Character. Geodesic engine to use when
\code{distance = "geodesic"}.}

\item{resistance_list}{Optional list of resistance rasters, either length 1
(reused for all times) or the same length as \code{points_list}.}

\item{dem_list}{Optional list of DEM rasters, either length 1 (reused for all
times) or the same length as \code{points_list}.}

\item{keep_polygons}{Logical. If \code{TRUE}, return polygons for each time step.}

\item{keep_summaries}{Logical. If \code{TRUE}, return summaries for each time step.}

\item{verbose}{Logical. If \code{TRUE}, prints progress.}

\item{...}{Additional arguments passed to \code{\link[=weighted_voronoi_domain]{weighted_voronoi_domain()}}.}
}
\value{
A list containing:
\describe{
\item{allocations}{A \code{terra::SpatRaster} with one allocation layer per time step.}
\item{time_index}{Character vector of time labels.}
\item{change_map_first_last}{A \code{terra::SpatRaster} indicating whether allocation
changed between the first and last time step (\code{1} = changed, \code{0} = unchanged).}
\item{persistence}{A \code{terra::SpatRaster} indicating whether each cell retained
the same allocation across all time steps (\code{1} = persistent, \code{0} = changed at least once).}
\item{polygons}{Optional list of \code{sf} polygon outputs by time.}
\item{summaries}{Optional list of summary tables by time.}
}
}
\description{
Runs weighted tessellation across a sequence of time-specific point datasets
and returns a stack of allocation rasters, with optional polygons and summaries
per time step.
}
\details{
This first implementation assumes a static boundary and runs each time step
independently. Time-varying weights, point locations, and resistance/DEM
surfaces are supported by supplying separate inputs for each time step.
}
