% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weightedVoronoi.R
\name{weighted_voronoi_geodesic}
\alias{weighted_voronoi_geodesic}
\title{Weighted geodesic tessellation (core)}
\usage{
weighted_voronoi_geodesic(
  points_sf,
  weight_col,
  boundary_sf,
  res = 20,
  weight_transform = function(w) w,
  close_mask = TRUE,
  close_iters = 1,
  dem_rast = NULL,
  use_tobler = TRUE,
  tobler_v0_kmh = 6,
  tobler_a = 3.5,
  tobler_b = 0.05,
  min_speed_kmh = 0.25,
  verbose = TRUE
)
}
\arguments{
\item{points_sf}{An \code{sf} POINT object containing generator locations and attributes.}

\item{weight_col}{Character. Name of the weight column in \code{points_sf}.}

\item{boundary_sf}{An \code{sf} POLYGON/MULTIPOLYGON defining the domain.}

\item{res}{Numeric. Raster resolution in CRS units (e.g. metres).}

\item{weight_transform}{Function. Transforms weights before allocation. Must return
finite, strictly positive values.}

\item{close_mask}{Logical. If \code{TRUE}, applies a morphological closing to the raster
mask (geodesic only).}

\item{close_iters}{Integer. Number of closing iterations (geodesic only).}

\item{dem_rast}{Optional SpatRaster providing elevation or resistance surface.
Must align with the tessellation domain and resolution.}

\item{use_tobler}{Logical; if TRUE, apply Tobler's hiking function to convert
slope into isotropic movement cost.}

\item{tobler_v0_kmh}{Base walking speed on flat terrain (km/h).}

\item{tobler_a}{Tobler exponential slope coefficient (default -3.5).}

\item{tobler_b}{Tobler slope multiplier (default 0.05).}

\item{min_speed_kmh}{Minimum allowed speed to avoid infinite costs.}

\item{verbose}{Logical. If \code{TRUE}, prints progress.}
}
\value{
A list containing polygon output, allocation raster, and weights.
}
\description{
Computes a weighted tessellation using domain-constrained (geodesic) distances.
Distances are calculated as shortest-path distances through a rasterised domain mask.
}
