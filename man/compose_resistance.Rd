% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weightedVoronoi.R
\name{compose_resistance}
\alias{compose_resistance}
\title{Compose a resistance surface from multiple raster layers}
\usage{
compose_resistance(
  ...,
  template = NULL,
  mask = NULL,
  method = c("multiply", "add", "max"),
  resample_method = c("bilinear", "near"),
  na_policy = c("propagate", "ignore")
)
}
\arguments{
\item{...}{One or more \code{terra::SpatRaster} layers. All layers must represent strictly
positive resistance values.}

\item{template}{Optional \code{terra::SpatRaster} defining the target grid (CRS, resolution,
extent). Defaults to the first layer.}

\item{mask}{Optional mask applied after alignment. Can be a \code{terra::SpatRaster},
\code{terra::SpatVector}, or \code{sf} polygon.}

\item{method}{How to combine layers: \code{"multiply"} (default), \code{"add"}, or \code{"max"}.}

\item{resample_method}{Resampling method used when aligning layers: \code{"bilinear"} for
continuous resistance, \code{"near"} for categorical rasters.}

\item{na_policy}{How to treat NA values: \code{"propagate"} makes any NA propagate to the
output; \code{"ignore"} drops NAs (neutral element for the chosen method).}
}
\value{
A \code{terra::SpatRaster} resistance surface on the template grid.
}
\description{
Aligns one or more \code{terra::SpatRaster} layers to a common template (CRS, resolution,
extent) and combines them into a single resistance raster using a specified rule.
Intended for building \code{resistance_rast} inputs for geodesic tessellations.
}
\examples{
\dontrun{
R <- compose_resistance(slope_resistance, landcover_resistance, method = "multiply")
R <- add_barriers(R, rivers_sf, permeability = "semi", cost_multiplier = 20, width = 30)
out <- weighted_voronoi_domain(points_sf, "w", boundary_sf, distance = "geodesic",
                               resistance_rast = R)
}
}
