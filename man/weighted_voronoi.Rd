% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weightedVoronoi.R
\name{weighted_voronoi}
\alias{weighted_voronoi}
\title{Weighted Euclidean tessellation (core)}
\usage{
weighted_voronoi(
  points_sf,
  weight_col,
  boundary = NULL,
  template_rast = NULL,
  res = NULL,
  weight_transform = function(w) w,
  method = c("argmin", "partition"),
  max_dist = NULL,
  verbose = TRUE,
  island_min_cells = 5,
  island_fill_iter = 50
)
}
\arguments{
\item{points_sf}{An \code{sf} POINT object containing generator locations and attributes.}

\item{weight_col}{Character. Name of the weight column in \code{points_sf}.}

\item{boundary}{Optional \code{sf} polygon defining the tessellation domain. Used when \code{template_rast} is \code{NULL}.}

\item{template_rast}{Optional \code{terra::SpatRaster} template raster. Provide this instead of \code{boundary} + \code{res}.}

\item{res}{Numeric. Raster resolution in CRS units (e.g. metres).}

\item{weight_transform}{Function. Transforms weights before allocation. Must return
finite, strictly positive values.}

\item{method}{Character. Allocation method; one of \code{"argmin"} or \code{"partition"}.}

\item{max_dist}{Optional numeric. Maximum Euclidean distance to consider (euclidean only).}

\item{verbose}{Logical. If \code{TRUE}, prints progress.}

\item{island_min_cells}{Integer. Minimum patch size used in island removal.}

\item{island_fill_iter}{Integer. Maximum iterations for filling reassigned cells.}
}
\value{
A list containing polygon output (if requested), allocation raster, and weights.
}
\description{
Internal/core function used by \code{\link[=weighted_voronoi_domain]{weighted_voronoi_domain()}} to compute a weighted
Euclidean tessellation on a rasterised domain.
}
