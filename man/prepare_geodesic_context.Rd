% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodesic_preparation_helpers.R
\name{prepare_geodesic_context}
\alias{prepare_geodesic_context}
\title{Prepare a geodesic context for repeated runs}
\usage{
prepare_geodesic_context(
  boundary_sf,
  res = 20,
  close_mask = TRUE,
  close_iters = 1,
  resistance_rast = NULL,
  dem_rast = NULL,
  use_tobler = TRUE,
  tobler_v0_kmh = 6,
  tobler_a = 3.5,
  tobler_b = 0.05,
  min_speed_kmh = 0.25,
  anisotropy = c("none", "terrain"),
  uphill_factor = 1,
  downhill_factor = 1,
  geodesic_engine = c("classic", "multisource")
)
}
\arguments{
\item{boundary_sf}{An \code{sf} POLYGON/MULTIPOLYGON defining the domain.}

\item{res}{Numeric. Raster resolution in CRS units (e.g. metres).}

\item{close_mask}{Logical. If \code{TRUE}, applies a morphological closing to the raster mask.}

\item{close_iters}{Integer. Number of closing iterations.}

\item{resistance_rast}{Optional SpatRaster giving movement resistance (>0).}

\item{dem_rast}{Optional SpatRaster providing elevation or resistance surface.}

\item{use_tobler}{Logical; if \code{TRUE}, apply Tobler's hiking function to convert slope into isotropic movement cost.}

\item{tobler_v0_kmh}{Base walking speed on flat terrain (km/h).}

\item{tobler_a}{Tobler exponential slope coefficient.}

\item{tobler_b}{Tobler slope multiplier.}

\item{min_speed_kmh}{Minimum allowed speed to avoid infinite costs.}

\item{anisotropy}{Character. One of \code{"none"} or \code{"terrain"}.}

\item{uphill_factor}{Numeric > 0. Additional uphill movement penalty when \code{anisotropy = "terrain"}.}

\item{downhill_factor}{Numeric > 0. Relative ease of downhill movement when \code{anisotropy = "terrain"}.}

\item{geodesic_engine}{Character. One of \code{"classic"} or \code{"multisource"}.}
}
\value{
A prepared geodesic context object for repeated geodesic allocation.
}
\description{
Precomputes the domain mask, aligned resistance handling, transition object,
and multisource graph representation (when applicable) for repeated geodesic
tessellation workflows.
}
